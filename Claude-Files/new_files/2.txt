Vou ler o skill de frontend antes de escrever as telas.Agora o patch pra conectar as abas no `CalculatorScreen.kt`:Os 3 arquivos estão prontos. Resumo do que foi feito:

**ConjuntosScreen.kt**
- Input de A, B e Universo (U só aparece quando seleciona Complemento via `AnimatedVisibility`)
- `FilterChip` pra cada operação (União/Interseção/Diferença/Complemento)
- Diagrama de Venn em `Canvas` puro — sem lib externa, com `BlendMode.Multiply` pra sobreposição real, contagem de elementos por região, labels A/B
- Card de passos numerados com `FontFamily.Monospace` (legível na prova)
- Card de resultado com chips coloridos por região (azul=A, roxo=A∩B, vermelho=B)

**MatrizesScreen.kt**
- Seletor de dimensão `2×2` até `3×3` via chips
- Grade editável com `BasicTextField` por célula — aceita negativos e decimais, regex limpa
- Colchetes `[ ]` decorativos ao redor da grade (visual matricial real)
- Desabilita Determinante/Inversa automaticamente pra matrizes não-quadradas
- Resultado com highlight de linha/coluna do último passo calculado
- Passos do Gauss-Jordan e Laplace em monospace, fundo destacado nos passos com destaque de posição

**CalculatorScreen_patch.kt**
- Só os 3 blocos que você precisa colar: imports, `NavigationBar` items, `when(tab)` cases

Ponto de atenção: o `BlendMode.Multiply` no Canvas funciona bem em fundo claro. Se o tema escuro do Android for aplicado, o `nativeCanvas.drawText` nos labels vai usar cor hardcoded — te recomendo passar a cor do `MaterialTheme` via parâmetro pro `DrawScope` se isso virar problema.